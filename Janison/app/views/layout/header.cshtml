<div class="header" ng-controller="HeaderCtrl as vm">
    <nav class="navbar navbar-default">
        <div class="navbar-header">
            <div class="visible-xs">
                <ul class="nav navbar-nav pull-right">
                    <li><a href="#">Total Courses (including disabled): {{vm.totalCourseCount}}</a></li>
                </ul>
            </div>
            <a class="navbar-brand" ui-sref="courses">Janison</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li ui-sref-active-if="courses"><a ui-sref="courses">Courses <span class="sr-only">(current)</span></a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="#">Total Courses (including disabled): {{vm.totalCourseCount}}</a></li>
            </ul>
        </div>
    </nav>
</div>